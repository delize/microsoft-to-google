# PST to Gmail Migration Tool - Python Dependencies
#
# Install with: pip install -r requirements.txt
#
# =============================================================================
# SYSTEM DEPENDENCIES (cannot be installed via pip)
# =============================================================================
# readpst (libpst) - Required for PST file conversion
#   - macOS:        brew install libpst
#   - Ubuntu/Debian: sudo apt install pst-utils
#   - Fedora/RHEL:  sudo dnf install libpst
#   - Arch:         sudo pacman -S libpst
#   - Windows:      Use WSL2 (see SETUP_READPST.md)
#
# =============================================================================
# PYTHON DEPENDENCIES (installed via pip from this file)
# =============================================================================

# GYB (Got Your Back) - Gmail backup/restore tool
# https://github.com/GAM-team/got-your-back
# NOTE: GYB cannot be pip installed - it's distributed as standalone builds
# Run: scripts/install_deps.sh to download automatically
# Or download from: https://github.com/GAM-team/got-your-back/releases

# Testing dependencies
pytest>=7.0.0
